# judge API whitelist — Phase 5 observation mode
read:
  - GET /v1/health
  - GET /v1/entries
  - GET /v1/entries/{id}
  - GET /v1/lookup/by-trigger
  - GET /v1/lookup/by-symptom
  - GET /v1/librarian/threads
  - GET /v1/librarian/threads/{id}/messages
  - GET /v1/librarian/tasks
write:
  - POST /v1/librarian/chat
  # Phase 5: drafts only. Actual entity writes are reserved for Phase 6.
